---
description: Disable snip; the master switch goes off and hooks pass output through unchanged
model: haiku
effort: low
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-run.sh" disable`

Relay the command output above to the user verbatim, inside a fenced code block. Add no preamble, commentary, or summary.
