---
description: Turn snip's optimization OFF (the master switch). Use only when the user explicitly asks to disable or turn off snip.
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-cmd.sh" disable`

The block above is the output of `snip disable`. Confirm the result to the user.
