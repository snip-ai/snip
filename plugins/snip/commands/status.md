---
description: Report whether snip is active and show its per-optimizer status and version. Use when the user asks if snip is on, enabled, running, or working.
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-cmd.sh" status`

The block above is the output of `snip status`. Answer the user from it, concisely.
