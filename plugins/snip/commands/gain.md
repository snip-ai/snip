---
description: Report snip's net token savings this session (input saved minus induced re-read cost). Use when the user asks how many tokens they saved, about snip's savings, or its impact.
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-cmd.sh" gain`

The block above is the output of `snip gain`. Answer the user from it, concisely.
