---
description: Check for the latest snip release and fetch the binary if newer. Use when the user asks to update snip or check for a new version.
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-cmd.sh" update`

The block above is the output of `snip update`. Relay the result to the user.
