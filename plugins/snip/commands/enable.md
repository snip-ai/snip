---
description: Turn snip's optimization ON (the master switch). Use only when the user explicitly asks to enable or turn on snip.
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-cmd.sh" enable`

The block above is the output of `snip enable`. Confirm the result to the user.
