---
description: Get, set, list, or reset snip configuration via dotted paths
argument-hint: "get|set|list|reset [path] [value]"
model: haiku
effort: low
disable-model-invocation: true
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-run.sh" config $ARGUMENTS`

Relay the command output above to the user verbatim, inside a fenced code block. Add no preamble, commentary, or summary.
