---
description: "Opt-in: add snip's binary dir to your shell PATH so you can run `snip` directly (or `remove` it)"
argument-hint: "[remove]"
model: haiku
effort: low
disable-model-invocation: true
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/snip-shell-setup.sh" $ARGUMENTS`

Relay the command output above to the user verbatim, inside a fenced code block. Add no preamble, commentary, or summary.
